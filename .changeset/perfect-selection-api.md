---
"@api-wrappers/anilist-wrapper": major
---

Add schema-derived field selection across every wrapper query and mutation with normalized response roots, legacy selection compatibility, current AniList mutation inputs, safer runtime validation, and refreshed generated API types.
