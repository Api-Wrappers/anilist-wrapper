---
"@api-wrappers/anilist-wrapper": minor
---

Add reference and read coverage with selection support: `media.getGenres()`, `media.getMediaTags()`, `media.getAiringSchedule()`, `media.getAiringSchedulesByMedia()`, a new `studio` service with `getStudioById()` and `searchStudios()`, and `user.getViewer()`/`getViewerStatistics()`.
