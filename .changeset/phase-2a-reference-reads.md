---
"@api-wrappers/anilist-wrapper": minor
---

Add reference and read coverage with selection support: `media.getGenres()`, `media.getMediaTags()`, `media.getAiringSchedule()`, `media.getAiringSchedulesByMedia()`, and `user.getViewer()`/`user.getViewerStatistics()`.
